import type { Metadata } from "next";
export const metadata: Metadata = { title: "교과서 열람 — MindPalace" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
