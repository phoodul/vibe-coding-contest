import type { Metadata } from "next";
export const metadata: Metadata = { title: "English Village — MindPalace" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
