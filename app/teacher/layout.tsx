import type { Metadata } from "next";
export const metadata: Metadata = { title: "교사 도구 — MindPalace" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
