import type { Metadata } from "next";
export const metadata: Metadata = { title: "기억의 궁전 — MindPalace" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
